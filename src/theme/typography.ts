import { moderateScale } from '../utils/responsive';

export const typography = {
  h1: moderateScale(28, 0.7),
  h2: moderateScale(22, 0.6),
  body: moderateScale(16),
  caption: moderateScale(12, 0.35),
};
